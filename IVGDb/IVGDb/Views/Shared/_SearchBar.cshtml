@using (Html.BeginForm("Results", "VideoGame", FormMethod.Get, new { id = "Search-form" }))
{
    <div class="form-group" id="Search-bar">
        <input type="text" class="form-control" id="Search-input" name="search" placeholder="Find Games, Developers, and more...">
        <button type="submit" class="btn btn-default" id="Search-btn"><i class="glyphicon glyphicon-search"></i></button>
    </div>
}